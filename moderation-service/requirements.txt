fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-dotenv==1.0.1

# Modèles Hugging Face
# - detoxify : checkpoint `multilingual` (XLM-RoBERTa, 7 catégories, FR natif)
# - transformers : modèles optionnels (sentiment, zero-shot, haine FR)
detoxify==0.5.2
transformers>=4.40
sentencepiece>=0.2.0

# Tests : pip install -r requirements-dev.txt
